import React from "react";
import classnames from "classnames";

import styles from "./Desk.module.scss";
// import "./Desk.scss";

const Desk: React.FC = () => {
  return (
    <h1 className={styles.desk}>SASSS</h1>
  );
};

export default Desk;
